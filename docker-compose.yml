# ============================================
# LOCAL DEVELOPMENT ONLY
# For Railway deploy, use root Dockerfile
# ============================================

version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cs2-ping-checker
    restart: unless-stopped
    ports:
      - "8080:8080"
    cap_add:
      - NET_RAW
    environment:
      - NODE_ENV=production
